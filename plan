// X - check to see if all relevant packages are installed, express, pg, nodemon & dotenv 
// X - create blank database on heroku
// X - create environment variables
// X - create a connection pool 
// X - get information to populate the tables - 5 x tech links
// X - set up our query function (pool.query) that talks to the database
// X - will plan structure of our table 
// X write the database scripts 
  X // 1 - create table 
  X // 2 - populate table 
// X ensure express is set up correctly and listening on port 
// X plan our routes using CRUD method 
// X write get route
// X go over SQL queries and make a few queries to test db -by lunch 
// X write post route
// X write put route
// X write patch route
// X write delete route
// X separate our functionality into models**
// X in the read.me = documentation 

// X refactor model code so it returns payload
// X make a drop table script 
// X query request 

21/06 ** separated routes that work into model, put and post router does not work so not added to models folder 